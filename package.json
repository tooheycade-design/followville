{
  "name": "followville",
  "version": "1.0.0",
  "private": true,
  "description": "Browser regression checks for the Followville public town.",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "company-os:typecheck": "pnpm --dir company-os typecheck",
    "company-os:test": "pnpm --dir company-os test",
    "company-os:simulate": "pnpm --dir company-os simulate",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:inventory": "node tests/inventory_panel_test.mjs && node tests/inventory_migration_test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  },
  "//optionalDevTools": [
    "test:inventory needs jsdom and @electric-sql/pglite, deliberately NOT in",
    "devDependencies: CI runs pnpm install --frozen-lockfile, and making the",
    "browser job download a wasm PostgreSQL on every run buys CI nothing.",
    "Install them once locally when you want that suite:",
    "  pnpm add -D --no-frozen-lockfile jsdom @electric-sql/pglite",
    "Both test files exit with that instruction if the import is missing."
  ]
}
