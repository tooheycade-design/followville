# Big render videos/previews — keep the repo light for GitHub Pages
renders/
*.mp4
*.png
!logo.png

# Blender backup file + old test states — not needed for the live site
*.blend1
world_state_*_backup.json
world_state_*_hold.json

# Supabase service-role key for the houses-table sync — SECRET, never commit/deploy
supabase_sync.env
# macOS + Python cruft
.DS_Store
__pycache__/

# iCloud sync conflict copies (e.g. "CLAUDE 7.md", "world_state 3.json") — sync
# artifacts, not real files. See CLAUDE.md's Third AI section for why these
# appear and how to recover from them if one ever holds unmerged content.
*[0-9].md
*[0-9].py
*[0-9].json

# One-off scratch scripts + debug logs from building/troubleshooting the deploy
# and growth tooling — safe to regenerate, not needed to reproduce the project.
# See CLAUDE.md's Files section for the full rationale on each of these.
*_log.txt
grow_log.txt
grow_step*.txt
grow_street.txt
grow_day8_*.txt
render_log_hero.txt
render_log_overhead.txt
check_push_status.*
check_remote.*
check_repo.*
clone_repo.bat
cleanup_procs.*
deploy_check.txt
git_config.*
git_install.txt
inspect_repo*.*
install_ci_check*.txt
list_folder.*
proc_check*.txt
pull_and_check.*
restore_canonical.*
test_git_pipeline*.txt
fix_and_*.bat
fix_reexport.bat
day8_shots.bat
day8_shots_done.txt
copy_videos_log.txt
deploy_log.txt
neighborhood_blender_prefix_backup_*.py
world_state_day*_backup_*.json

# Local-only admin tool — reads the secret service-role key at runtime, must
# never be deployed (see CLAIMING_SETUP.md)
admin.bat
